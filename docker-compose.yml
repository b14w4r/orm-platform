services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: orm
      POSTGRES_USER: orm
      POSTGRES_PASSWORD: orm
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
